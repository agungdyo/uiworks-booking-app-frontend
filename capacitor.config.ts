import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.uipay.uiworks',
  appName: 'UIWorks Coworking Space',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
